{
    "name": "frey-day020",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node scripts/dev-hotreload.mjs",
        "build": "npm run wasm:build && vite build",
        "preview": "vite preview",
        "wasm:build": "wasm-pack build rust --target web --out-dir ../src/wasm --release",
        "wasm:build:dev": "wasm-pack build rust --target web --out-dir ../src/wasm --dev"
    },
    "dependencies": {
        "three": "^0.180.0"
    },
    "devDependencies": {
        "vite": "^7.1.9"
    }
}
